openapi: 3.0.3
info:
  title: Play with goa
  description: HTTP service for playing with goa
  version: "1.0"
servers:
- url: http://localhost:8080
paths:
  /add/{a}/{b}:
    get:
      tags:
      - play
      summary: add play
      operationId: play#add
      parameters:
      - name: a
        in: path
        description: Left operand
        required: true
        schema:
          type: integer
          description: Left operand
          example: 1
          format: int64
        example: 1
      - name: b
        in: path
        description: Right operand
        required: true
        schema:
          type: integer
          description: Right operand
          example: 2
          format: int64
        example: 2
      responses:
        "200":
          description: OK response.
          content:
            application/json:
              schema:
                type: integer
                example: 3
                format: int64
              example: 3
  /rate/{id}:
    post:
      tags:
      - play
      summary: rate play
      operationId: play#rate
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          example: 1
          format: int64
        example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              example:
                a: 1.1
                b: 2.2
              additionalProperties: true
            example:
              a: 1.1
              b: 2.2
      responses:
        "204":
          description: No Content response.
components: {}
